@{
    ViewData["Title"] = "Departments Management";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    var departments = ViewBag.Departments as List<Estately.WebApp.ViewModels.DepartmentViewModel> ?? new();
    var page = (int)(ViewBag.Page ?? 1);
    var pageSize = (int)(ViewBag.PageSize ?? 10);
    var totalPages = (int)(ViewBag.TotalPages ?? 1);
    var searchTerm = ViewBag.SearchTerm as string;
}

<div class="table-container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4>Departments Management</h4>
        <a asp-action="CreateDepartment" asp-controller="Admin" class="btn btn-admin">
            <i class="bi bi-plus-circle me-2"></i>Add New Department
        </a>
    </div>

    <form method="get" asp-action="Departments" asp-controller="Admin" class="mb-4">
        <div class="row">
            <div class="col-md-8">
                <input type="text" name="searchTerm" class="form-control" placeholder="Search departments..." value="@searchTerm" />
            </div>
            <div class="col-md-2">
                <input type="number" name="pageSize" class="form-control" value="@pageSize" min="5" max="100" />
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-admin w-100">Search</button>
            </div>
        </div>
    </form>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Department Name</th>
                    <th>Manager</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var dept in departments)
                {
                    <tr>
                        <td>@dept.DepartmentID</td>
                        <td>@dept.DepartmentName</td>
                        <td>@(dept.ManagerName ?? "N/A")</td>
                        <td>@(dept.Email ?? "N/A")</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a asp-action="EditDepartment" asp-controller="Admin" asp-route-id="@dept.DepartmentID" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form method="post" asp-action="DeleteDepartment" asp-controller="Admin" asp-route-id="@dept.DepartmentID" class="d-inline" 
                                      onsubmit="return confirm('Are you sure?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (totalPages > 1)
    {
        <nav>
            <ul class="pagination justify-content-center">
                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == page ? "active" : "")">
                        <a class="page-link" asp-action="Departments" asp-controller="Admin" 
                           asp-route-page="@i" asp-route-pageSize="@pageSize" asp-route-searchTerm="@searchTerm">@i</a>
                    </li>
                }
            </ul>
        </nav>
    }
</div>


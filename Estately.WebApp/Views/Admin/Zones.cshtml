@{
    ViewData["Title"] = "Zones Management";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var zones = ViewBag.Zones as List<Estately.Services.ViewModels.ZoneViewModel> ?? new();
    var cities = ViewBag.Cities as SelectList ?? new SelectList(new List<object>());
    var page = (int)(ViewBag.Page ?? 1);
    var pageSize = (int)(ViewBag.PageSize ?? 10);
    var totalPages = (int)(ViewBag.TotalPages ?? 1);
    var searchTerm = ViewBag.SearchTerm as string;
}

<div class="table-container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4>Zones Management</h4>
        <a asp-action="CreateZone" asp-controller="Admin" class="btn btn-admin">
            <i class="bi bi-plus-circle me-2"></i>Add New Zone
        </a>
    </div>

    <form method="get" asp-action="Zones" asp-controller="Admin" class="mb-4">
        <div class="row">
            <div class="col-md-8">
                <input type="text" name="searchTerm" class="form-control" placeholder="Search zones..." value="@searchTerm" />
            </div>
            <div class="col-md-2">
                <input type="number" name="pageSize" class="form-control" value="@pageSize" min="5" max="100" />
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-admin w-100">Search</button>
            </div>
        </div>
    </form>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Zone Name</th>
                    <th>City</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var zone in zones)
                {
                    <tr>
                        <td>@zone.ZoneID</td>
                        <td>@zone.ZoneName</td>
                        <td>@(zone.CityName ?? "N/A")</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a asp-action="EditZone" asp-controller="Admin" asp-route-id="@zone.ZoneID" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form method="post" asp-action="DeleteZone" asp-controller="Admin" asp-route-id="@zone.ZoneID" class="d-inline" 
                                      onsubmit="return confirm('Are you sure?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (totalPages > 1)
    {
        <nav>
            <ul class="pagination justify-content-center">
                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == page ? "active" : "")">
                        <a class="page-link" asp-action="Zones" asp-controller="Admin" 
                           asp-route-page="@i" asp-route-pageSize="@pageSize" asp-route-searchTerm="@searchTerm">@i</a>
                    </li>
                }
            </ul>
        </nav>
    }
</div>

